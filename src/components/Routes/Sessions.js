import React from "react";
import Container from 'react-bootstrap/Container';

const Sessions = () => {
    return (
        <Container>
            <h1>Sessions</h1>
        </Container>
    );
};

export default Sessions;